/**
 * Dashboard Constants
 * Centralized configuration for Dashboard page timeouts and polling intervals
 */

// Polling intervals
export const AUDIT_POLL_INTERVAL_MS = 5000; // Poll in-progress audits every 5 seconds

// Toast durations
export const TOAST_DURATION_MS = 5000; // Standard toast display duration

// Navigation delays
export const NAVIGATION_DELAY_MS = 3000; // Delay before redirecting to login

// Dialog delays
export const DIALOG_CLOSE_DELAY_MS = 1500; // Delay before closing progress dialog

// Loading state delays
export const LOADING_DELAY_MS = 1000; // Minimum loading state display time
